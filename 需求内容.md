我需要写一个python程序,包括后端服务和前端服务，使用前后端分离的方式，前端使用vue+element-ui，后端使用flask+sqlLite。我需要这个web系统实现的功能有账号注册登录。需要完成的功能有数据源管理配置：连接数据源，数据源需要支持好的类型扩展，现阶段先支持hive，kingbase8即可。elt配置功能：1.支持不同数据库数据之间的数据迁移、字段类型名称转换、时间范围选择。2.同类型数据库表支持1中的全部功能，还需要支持表连接join操作在进行数据迁移操作。调度配置功能：针对etl配置功能，需要支持定时任务，支持任务依赖关系，支持任务失败重试。
